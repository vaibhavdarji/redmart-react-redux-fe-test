import React from 'react';

const NotFound = () => {
  <p className="not-found">
    Sorry, the page you are looking for does not exist. Click above to go back
    to the shop.
  </p>;
};

export default NotFound;
